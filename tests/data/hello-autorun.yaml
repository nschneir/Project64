name: hello-autorun
machine: c64
program: ../programs/hello-basic/program.bas
timeout: 30
steps:
  - wait: { text: "HELLO FROM BASIC" }
  - wait: { text: "2+2= 4" }
  - assert: { screen: "READY." }
  - assert: { reg: pc, in_range: ["$0000", "$FFFF"] }
