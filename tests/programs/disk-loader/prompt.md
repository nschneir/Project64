Write a Commodore 64 program that boots from a disk and loads a second file
(`DATA`) off the same disk at runtime, then prints its contents. The data file
must decide its own load address, and the program must report the KERNAL error
code if the load fails. Package it all as a `.d64`.
